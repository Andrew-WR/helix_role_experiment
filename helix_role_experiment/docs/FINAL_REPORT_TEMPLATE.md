# Final report: functional role of the candidate low-frequency subspace

## Evidence status

- Study stage:
- Models and immutable revisions:
- Pre-registration hash/date:
- Completed sample / exclusions:
- Engineering-only outputs excluded from inference:

## Executive conclusion

Select exactly one primary classification and justify it with the locked
criteria:

- semantic task progress;
- generic procedural phase;
- sequence clock / iteration count;
- expected sequence completion;
- termination or answer-emission readiness;
- confidence / commitment;
- mixed representation;
- diagnostic correlate or analysis artifact;
- narrow task-family-specific controller.

Do not use “progress representation” unless all six semantic criteria pass.

## Original-result audit

Report tautological phase recovery, spectral concentration versus each null,
endpoint/detrending sensitivity, plane stability, eigengaps, and missing
original-paper deviations.

## Frozen shared subspace

Compare Grassmann, generalized-eigen, and complex models on validation and
locked test tasks. Report full covariance whitening, random-plane comparison,
dimensionality checks, layer profile, radius exclusions, and cost.

## Observational crossed results

Report problem-level `Δ_progress`, `Δ_position`, and `Δ_termination`, with
uncertainty. Report nested position, completion, EOS, procedure, confidence,
semantic, and mixed models. Separate structural progress from behavioral
competence.

## Counterfactual event tests

Summarize padding invariance, same-length progress, teleport, rollback, loops,
answer-known/work-unfinished, answer permission, and matched reasoning modes.
Include failures and family heterogeneity.

## Sentence causal structure

Report present-versus-replacement sentence importance and causal-link
approximation, controlling for position, sentence length, and likelihood.

## Interventions and causal abstraction

Report layer/time localization, rotations, donor transplants, full-frame
interchange, content preservation, valid next-state changes, persistence, and
all norm/KL-matched controls.

## EOS exclusion

Report EOS-gradient overlap, EOS-orthogonalized effects, fixed-length results,
EOS-logit-matched results, direct EOS-bias comparisons, and answer-permission
crosses. State whether any effect beyond termination survives.

## Base versus helix-fine-tuned model

Report plane alignment, dynamics, EOS timing, reasoning-category durations,
removed content, verification/correction loss, objective accuracy, temperature
robustness, length-matched controls, and accuracy/token Pareto frontier.

## Criterion ledger

| Semantic criterion | Pass/fail/inconclusive | Locked result |
|---|---|---|
| Invariance | | |
| Teleport/loop/rollback sensitivity | | |
| Position independence | | |
| Content-preserving causal interchange | | |
| EOS independence | | |
| Cross-family robustness | | |

## Negative and mixed findings

Describe artifact/null results and unique contributions without relabeling them
as failures. Identify the most diagnostic next experiment.

## Reproducibility

List exact commands, config hashes, seeds, environment, shard completeness,
code revision, model/tokenizer revisions, and artifact checksums.

